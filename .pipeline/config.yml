general:
steps:
  mtaBuild:
    buildTarget: 'CF'
  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      org: 'a3b6db27trial'
      space: 'dev'
      credentialsId: 'CF_CREDENTIALSID'
